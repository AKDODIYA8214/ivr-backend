export const getAllIvrPaginationService = async (limit: string, offset: string) => {
  const data: any[] = [];
  const count = 0;
  return { data: data, count: count };
};
export const getAllIvrService = async () => {
  const data: any[] = [];
  const count = 0;
  return { data: data, count: count };
};
